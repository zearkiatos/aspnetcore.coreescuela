
<button class="btn btn-primary" onclick='location.href="@Url.Action("Create")"'>Crear</button>
<table class="table table-dark table-hover">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Edit</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                <a href="@Url.Action("Index")/@item.Id" class="text-reset text-decoration-none"><i class="fas fa-eye"></i></a> |
                   <a href="@Url.Action("Edit")/@item.Id" class="text-reset text-decoration-none"><i class="fas fa-edit edit" ></i></a> |
                   <a href="@Url.Action("Delete")/@item.Id" class="text-reset text-decoration-none"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>